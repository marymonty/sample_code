This directory contains work from my Network Programming class.

The SimpleMultithreading file shows a use of multithreading through an addition struct that adds two integers together.

The client and server files set up a network of nodes that can comunicate with one another if they are close enough in the network. 
